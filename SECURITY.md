# 安全策略

## 支持范围

| 范围 | 是否接收安全修复 |
| --- | --- |
| `master` 与最新源码发布 | 是 |
| 旧提交、旧源码发布、第三方 Fork | 不保证 |

iOSSignKit 初版仅发布源码，不发布官方预编译 `.app` 或 `.dmg`。第三方或自行构建的二进制不作为官方分发产物，但由本仓库源码直接导致的安全问题仍可报告。

## 私密报告漏洞

请使用 GitHub Private Vulnerability Reporting 私密提交报告：

https://github.com/WesleyXuZzz/ios-sign-kit/security/advisories/new

不要在公开 Issue、Discussion 或 Pull Request 中披露尚未修复的漏洞、攻击步骤、设备标识、Provisioning Profile、证书信息、账号信息、本机路径或完整日志。

报告建议包含：

- 受影响的提交或源码版本
- 可复现的前置条件和最小步骤
- 实际结果、预期结果及影响范围
- 已完成脱敏的日志或验证材料
- 建议的缓解或修复方向（如有）

维护者计划在 7 天内确认收到报告，并在 14 天内提供初步评估。修复和公开时间将根据影响、复现稳定性及协调披露需要确定。

如果私密报告入口暂不可用，请只通过公开 Issue 请求建立私密联系，不要公开漏洞细节。

## 安全范围与信任边界

以下问题属于本项目安全范围：

- 命令参数注入、任意宿主命令执行或意外执行其他项目
- 路径遍历、符号链接逃逸或越界读写
- 部署目标、设备、Scheme、Target、Bundle ID 或 Team 身份校验绕过
- Provisioning Profile 缓存隔离、回滚或崩溃恢复错误
- 已签名 App、嵌入 Profile、设备授权或代码签名核验绕过
- 部署进程归属、取消或异常恢复造成的越权终止
- 局域网控制的来源限制、身份认证、会话或配对令牌绕过
- 日志、回执、设备标识或签名材料的非预期泄露
- 有界输入、输出或文件限制失效导致的安全影响

## 核心安全不变量

- 只有经过完整身份核验的设备与 App Target 才能进入部署。
- Project 或 Workspace、Scheme、Target、Bundle ID、Team 和最终构建设置必须精确一致。
- 部署不依赖目标仓库提供 iOSSignKit 专用脚本。
- 每次部署使用独立的精确令牌、受控 DerivedData 和可验证的进程归属。
- Profile 缓存操作只允许作用于精确匹配的 Team 与 Bundle ID，并必须支持失败回滚和异常恢复。
- 安装前必须核验 App 路径、Bundle ID、可执行文件、代码签名、Team、设备授权、Profile 摘要和有效期。
- 无法证明进程、路径、设备或签名材料归属时必须失败关闭。

## 已知限制与已接受风险

- 用户选择的 Xcode 项目属于受信任输入。Xcode 构建可能执行该项目自带的 Build Phase、插件或其他构建逻辑，iOSSignKit 不对目标项目进行沙箱隔离。若 iOSSignKit 错误选择或构建了用户未授权的项目，仍属于可报告问题。
- 局域网控制默认关闭，并使用明文 HTTP，只适合受信任的本地网络。仅报告“未提供 TLS”本身不视为漏洞；身份认证绕过、来源限制绕过、凭据或会话泄露仍属于安全问题。
- 构建、签名与安装依赖 macOS、Xcode、CoreDevice 和 Apple 开发者账号状态。上游工具自身的问题不由本项目修复，但本项目对其结果的错误解析或不安全调用仍属于报告范围。
- 默认本地构建使用 ad-hoc 签名且未公证。初版不提供官方二进制，因此第三方二进制的签名、来源和完整性不由项目维护者背书。

## 严重性判断

严重性将结合可利用性、所需权限、影响范围和用户交互判断：

- 可导致未授权宿主代码执行、签名材料泄露或向错误设备安装 App 的问题通常按严重或高危处理。
- 局域网认证绕过、路径边界逃逸、签名或 Profile 核验绕过通常至少按高危评估。
- 需要本机已授权访问、复杂用户交互或影响受限的问题，根据实际影响按中危或低危评估。

## 不在本项目直接处置范围

- 与 iOSSignKit 集成方式无关的 macOS、Xcode、iOS 或 CoreDevice 上游漏洞
- 用户主动选择的恶意目标项目中原本存在的构建脚本或代码
- 第三方修改版、非官方二进制或未使用当前源码构建的产物
- 不具有安全影响的一般功能、兼容性或界面问题

上述排除项如果揭示了 iOSSignKit 的校验绕过、越权行为或错误信任边界，仍可作为安全问题报告。
